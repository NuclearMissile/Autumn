# autumn configuration:
autumn:
  config-class-name: # org.example.autumn.hello.HelloConfiguration
  datasource:
    url: jdbc:sqlite:test_jdbc.db
    driver-class-name: org.sqlite.JDBC
    username:
    password:

# server configuration:
server:
  host: localhost
  port: 8080

  # the only one web app:
  web-app:
    name: Test
